version: '3.8'

services:
  flask-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: flask-api:1.0 
    ports:
      - "19020:19020"
    environment:
      FLASK_APP: run.py
      FLASK_ENV: development
    volumes:
      - /home/dp/nifi_be4/DE31-final-team1/BE:/home/mydir/api-server
    env_file:
      - .env
